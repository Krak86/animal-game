/**
 * Game settings and configuration constants
 */

// Number of animal cards to display per round
export const ANIMALS_PER_SCREEN = 6;

// Number of animal pairs for memory matching mode (3 pairs = 6 tiles)
export const PAIRS_PER_SCREEN = 3;
export const TILES_PER_SCREEN = PAIRS_PER_SCREEN * 2; // 6 tiles total
